{
  "name": "asml-twinscan-web",
  "private": true,
  "version": "1.0.0",
  "description": "Three.js educational dual-stage EUV lithography simulator, using the original Blender asset.",
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "sync-assets": "node tools/sync-assets.mjs",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test"
  },
  "dependencies": { "three": "0.186.0" },
  "devDependencies": { "@playwright/test": "1.63.0", "vite": "8.3.0" }
}
